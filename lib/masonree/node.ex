defmodule Masonree.Node do
  @moduledoc """
  Defines one instance of a block in a document.

  A document is a tree of nodes and nothing else, which is what lets the tree be
  the only thing in a database.
  """
  @moduledoc since: "0.3.0"
end
