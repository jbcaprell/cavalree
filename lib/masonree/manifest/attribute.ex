defmodule Masonree.Manifest.Attribute do
  @moduledoc """
  Defines one attribute of a block.

  An attribute is the only part of a manifest that fixes the meaning of stored
  values: a node's attributes hold values whose meaning is declared here, and
  the rest of a manifest is code.
  """
  @moduledoc since: "0.3.0"
end
