defmodule Masonree.Block do
  @moduledoc """
  Defines what a block must answer.

  A block is a module rather than a row: what it declares is fixed when the
  library compiles, so no question about a block needs a database to answer it.
  """
  @moduledoc since: "0.3.0"
end
