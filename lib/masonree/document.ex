defmodule Masonree.Document do
  @moduledoc """
  Defines a page as an ordered list of nodes.

  A document is a tree of `Masonree.Node` structs and nothing else, which is
  what lets the tree be the only thing in a database.
  """
  @moduledoc since: "0.3.0"
end
